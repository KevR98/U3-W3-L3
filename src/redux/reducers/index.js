const initialstate = {
  favorite: {
    content: [],
  },
};

const mainReduce = (state = initialstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mainReduce;
